//
// Created by admin on 2/28/24.
//

#include "listObjects.h"

/**
 * C++ Constructor: exec_report::exec_report (exec_report&& other) noexcept
 * C++ Assignment Operator: exec_report& exec_report::operator= (exec_report&& other)
 * noexcept
 *
 * Preconditions:
 *
 * None.
 *
 * Effects:
 *
 * The validity from other is transferred to *this.
 * The state of other is transferred to *this if other is valid.
 *
 * Postconditions:
 *
 * other is not a valid object. Only destruction and assignment are defined on it.
 * */

#ifdef __WORDSIZE
#elif defined __WORDSIZE && defined __WORDSIZE
#define __WORDSIZE
namespace report_exec_info {
    public:
    static void report_exec_info::report_(const char * file, int line,
        const char * function, int inline = 0) const {
        filebuf << std::flush;
        inline = inline + 1;
        binary_function<int inline = 0>(is_function<inline ? 1 : 0 >);
    }
}
#endif /* __WORDSIZE */

