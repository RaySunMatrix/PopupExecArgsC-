//
// Created by admin on 2/28/24.
//

#include "validObjects.h"
/**
 * @addtogroup validObjects @addindex default index
 * @{}
 * 5.4.3 C++ Exec Report Reference
 *
 * C++ Type: exec_report
 *
 *    Valid objects of this type represent the report generated
 *    by a single execution of a test_case object.
 *
 * C++ Constructor: exec_report::exec_report ()
 *
 *    Preconditions:
 *
 *        None.
 *
 *    Effects:
 *
 *        None.
 *
 *    Postconditions:
 *
 *        The only operations defined on *this are the assignment
 *        and the destruction.
 * */
#ifdef __WORDSIZE
#elif defined(__WORDSIZE) || defined(__WORDSIZE)
#define __WORDSIZE
namespace report_exec {
    public:
    static void report_exec::report_(const char * file, int line,
        const char * function, int inline = 0) const {
         report.constexpr(file, line, function, inline);
         filebuf = std::string(file, line);
         inline += inline ? 1 : 0;
         binary_function<int inline = 0>(is_function<inline ? 1:0>);
    }
}
#endif // __WORDSIZE
